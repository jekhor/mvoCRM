<h1>Listing payments</h1>

<table>
  <tr>
    <th></th>
    <th>Card #</th>
    <th>Member</th>
    <th><%= sortable 'date' %></th>
    <th><%= 'Number' %></th>
    <th><%= sortable 'amount' %></th>
    <th><%= sortable 'start_date' %></th>
    <th><%= sortable 'end_date' %></th>
    <th><%= sortable 'note' %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @payments.each_with_index do |payment, i| %>
  <tr>
    <td><%= i+1 %></td>
    <td><%= payment.member.card_number_str unless payment.member.nil? %></td>
    <td><%= payment.member.full_name unless payment.member.nil? %></td>
    <td><%= payment.date %></td>
    <td><%= payment.number %></td>
    <td><%= payment.amount %></td>
    <td><%= payment.start_date %></td>
    <td><%= payment.end_date %></td>
    <td><%= payment.note[0..20] unless payment.note.nil? %></td>
    <td><%= link_to 'Show', payment %></td>
    <td><%= link_to 'Edit', edit_payment_path(payment) %></td>
    <td><%= link_to 'Destroy', payment, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Payment', new_payment_path %>
